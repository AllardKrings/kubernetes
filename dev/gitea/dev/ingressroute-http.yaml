apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: gitea-http
  namespace: gitea
spec:
  entryPoints:
    - web
  routes:
  - match: Host(`gitea-dev.allarddcs.nl`)
    kind: Rule
    services:
    - name: gitea
      port: 3000
